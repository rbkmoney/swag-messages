type: object
required:
  - conversations
  - messages
  - status
properties:
  conversations:
    type: array
    items:
      $ref: '#/definitions/Conversation'
  users:
    type: object
    additionalProperties:
      $ref: '#/definitions/User'
