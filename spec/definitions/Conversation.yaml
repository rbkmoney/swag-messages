type: object
required:
  - conversationId
  - messages
  - status
properties:
  conversationId:
    $ref: '#/definitions/ConversationID'
  messages:
    type: array
    items:
      $ref: '#/definitions/Message'
  status:
    $ref: '#/definitions/ConversationStatus'
