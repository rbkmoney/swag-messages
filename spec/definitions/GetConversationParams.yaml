type: object
required:
  - conversationsIds
properties:
  conversationsIds:
    type: array
    items:
      $ref: '#/definitions/ConversationID'
  filter:
    $ref: '#/definitions/ConversationFilter'
