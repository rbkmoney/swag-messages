type: object
required:
  - messageId
  - text
  - userId
  - timestamp
properties:
  messageId:
    $ref: '#/definitions/MessageID'
  text:
    type: string
  userId:
    $ref: '#/definitions/UserID'
  timestamp:
    type: string
