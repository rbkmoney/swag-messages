type: object
required:
  - conversationsIds
properties:
  conversations:
    type: array
    items:
      $ref: '#/components/schemas/Conversation'
  users:
    type: array
    items:
      $ref: '#/components/schemas/User'
